# Porta HTTP do servidor
PORT=8080

# Conexão SQLite (driver pure-Go modernc.org/sqlite)
DATABASE_URL=file:chamados.db?_pragma=foreign_keys(1)

# Segredo de assinatura dos JWT — TROQUE em produção por um valor forte e aleatório
JWT_SECRET=dev-secret-change-me
